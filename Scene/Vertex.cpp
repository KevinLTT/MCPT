#include "Vertex.h"

/*glm::vec3 Vertex::getPosition()
{
    return position;
}

float Vertex::getPosition( int axis )
{
    switch( axis )
    {
    case X_AXIS:
        return position.x;
    case Y_AXIS:
        return position.y;
    case Z_AXIS:
    default:
        return position.z;
    }
}

glm::vec3 Vertex::getNormal()
{
    return normal;
}

float Vertex::getNormal( int axis )
{
    switch( axis )
    {
    case X_AXIS:
        return normal.x;
    case Y_AXIS:
        return normal.y;
    case Z_AXIS:
    default:
        return normal.z;
    }
}*/
